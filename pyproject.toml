[project]
name = "llmutex"
version = "0.1.0"
description = "A mutex-like API for leasing LLM resource access."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.139.2",
    "pydantic>=2.13.4",
]

[project.scripts]
llmutex = "src.main:main"
